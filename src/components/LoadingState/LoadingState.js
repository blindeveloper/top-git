import React from 'react';
import { StyledLoadingState } from './StyledLoadingState';

const LoadingState = () => {
  return <StyledLoadingState>LOADING...</StyledLoadingState>;
};

export default LoadingState;
